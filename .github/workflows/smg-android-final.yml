name: SMG Android Final Build
# Trigger revision 2

permissions:
  contents: write

on:
  workflow_dispatch:
  push:
    paths:
      - '.github/workflows/smg-android-final.yml'
      - 'scripts/smg_*'

jobs:
  build:
    runs-on: windows-latest
    timeout-minutes: 120
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Prepare original game and tools
        shell: pwsh
        run: ./scripts/smg_prepare.ps1

      - name: Compile tailored touch controls
        shell: pwsh
        run: ./scripts/smg_inject.ps1

      - name: Build Android package
        shell: pwsh
        run: ./scripts/smg_package.ps1

      - name: Sign Android package
        shell: pwsh
        run: ./scripts/smg_sign.ps1

      - name: Verify final APK
        shell: pwsh
        run: ./scripts/smg_verify.ps1

      - name: Upload final APK
        uses: actions/upload-artifact@v4
        with:
          name: Super-Monsters-n-Girls-v2.0.2-Android-v1
          path: dist/Super-Monsters-n-Girls-v2.0.2-Android-v1.apk
          compression-level: 0
          retention-days: 7

      - name: Upload QA reports
        uses: actions/upload-artifact@v4
        with:
          name: SMG-Android-QA
          path: |
            work/original-sha256.txt
            work/version-run.txt
            work/mobile-injection.log
            work/control-verification.txt
            work/aapt-badging.txt
            work/signature.txt
            work/zipalign-check.txt
            work/apk-zip-test.txt
            work/final-game-info.txt
            work/final-hash.txt
          retention-days: 30

      - name: Publish APK release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: smg-android-v1-final
          name: "Super Monsters'n Girls v2.0.2 Android v1"
          body: |
            Android port built from the supplied GameMaker 2.2.2.302 VM release.
            Touch controls: movement, Attack, Jump, Enter/Start, Menu/Escape.
          files: dist/Super-Monsters-n-Girls-v2.0.2-Android-v1.apk
          overwrite_files: true
